import { Colors as BaseColors } from '@/constants/theme';

// Lightweight theme tokens used by the app screens.
// For now these pick the light theme values — we can extend to support dark mode later.
export const Colors = {
  background: BaseColors.light.background,
  textPrimary: BaseColors.light.text,
  textSecondary: BaseColors.light.icon,
  primary: BaseColors.light.tint,
  card: '#FFFFFF',
};
